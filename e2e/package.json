{
	"name": "ultralight-carousel-via-sse-e2e",
	"private": true,
	"description": "End-to-end suite. Drives a real browser against a real WordPress site.",
	"scripts": {
		"test": "playwright test --project=desktop --project=firefox",
		"test:all": "docker run --rm --network host -u $(id -u):$(id -g) -e BASE_URL -e HOME=/tmp -v \"$PWD\":/work -w /work mcr.microsoft.com/playwright:v1.62.1-noble npx playwright test",
		"report": "playwright show-report"
	},
	"devDependencies": {
		"@playwright/test": "1.62.1",
		"@axe-core/playwright": "^4.10.0"
	}
}
